# 第3章：ブランチの命名規則（Branch Naming Rules）

チーム開発では、各自が役割に応じたブランチ（作業部屋）を作って並行して作業します。
誰がどのような目的でそのブランチを作ったのかが全員に一目で伝わるよう、チームごとにルール（命名規則）を定めて運用します。

---

## 3.1 なぜ命名規則が必要なのか？

1. **作業内容の予測可能性**:
   ブランチ名を見るだけで、「新機能を作っているのか」「バグを直しているのか」が第三者にも判断できます。
2. **整理整頓**:
   GitHub上でブランチ一覧を見たときに、カテゴリごとに整理されて見やすくなります。
3. **自動化ツール（CI/CD）との連携**:
   「`release/` から始まるブランチがプッシュされたら自動で本番環境にデプロイする」といった設定が簡単に行えるようになります。

---

## 3.2 代表的なプレフィックス（接頭辞）

一般的には、ブランチ名の先頭に **`カテゴリ/`** をつける形式が採用されます。

| プレフィックス | 目的 | 具体的な用途 |
| :--- | :--- | :--- |
| **`feature/`** | 新機能の開発 | 画面の追加、新しいAPIの実装など |
| **`bugfix/`** | 不具合の修正 | 既存機能のバグ、エラーの修正 |
| **`hotfix/`** | 緊急の修正 | 本番環境で今すぐ修正が必要な重大なバグ対応 |
| **`docs/`** | ドキュメントの修正 | READMEの作成、仕様書の記述変更など |
| **`chore/`** | 雑務・設定変更 | ライブラリのアップデート、ビルド設定の変更など |

---

## 3.3 命名のルールと具体例

ブランチ名は、**「カテゴリ/イシュー番号-簡潔な英語の作業内容」**にするのが最も推奨されるパターンです。

* **良い例（Good）**:
  * `feature/#12-user-login`（イシュー #12 のユーザーログイン機能開発）
  * `bugfix/#45-payment-error`（イシュー #45 の決済エラー修正）
  * `docs/update-readme`（READMEの更新作業）

* **悪い例（Bad）**:
  * `my-work`（誰の何の作業か全くわからない）
  * `fix`（どのバグを直しているかわからない）
  * `feature/ログイン画面をつくる`（日本語やスペース、全角文字はエラーや文字化けの原因になるため避ける）

> [!IMPORTANT]
> **ブランチ名作成時の絶対ルール**
> 1. **すべて半角の小文字（lower case）を使用する**
>    MacやWindowsはフォルダやファイルの「大文字・小文字」を区別しないことがありますが、Linux（GitHubのサーバー）は厳密に区別します。大文字が混ざると、「自分のPCでは動くのにGitHubにプッシュするとエラーになる」というトラブルを引き起こすため、必ずすべて小文字にします。
> 2. **記号はハイフン（`-`）とスラッシュ（`/`）のみを使用する**
>    スペースや日本語、特殊文字はGitのコマンドエラーの原因になります。

---

* [前へ（第2章：Gitの基本概念・コマンド・.gitignore）](02_git_basic_concepts.md)
* [総合目次に戻る](git_team_development_guide.md)
* [次の章（第4章：チーム開発のワークフロー）へ進む](04_team_development_flow.md)
